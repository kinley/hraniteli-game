<% form_for(@message) do |f| %>
	<%= f.label "текст сообщения" %>
	<%= f.text_area :text %>
	
	<%= select("message", "user_id", User.find(:all, :order => "name").collect {|c| [ c.name, c.id ] }, { :prompt => ('Отправить всем')}) %>
  
	<%= f.submit "Отправить" %> 
<% end %>